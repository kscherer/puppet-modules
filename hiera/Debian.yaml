---
packages:
  - texi2html
  - chrpath
  - diffstat
  - subversion
  - libgl1-mesa-dev
  - libglu1-mesa-dev
  - libsdl1.2-dev
  - texinfo
  - gawk
  - gcc
  - gcc-multilib
  - help2man
  - g++
  - git-core
  - python-gtk2
  - bash
  - diffutils
  - xz-utils

#Enable the smux extension on debian
snmp::snmpd_options: '-Lsd -Lf /dev/null -u snmp -g snmp -I smux -p /var/run/snmpd.pid'

java::distribution: 'openjdk-7-jre-headless'

apt::purge_sources_list: true
apt::purge_sources_list_d: true
apt::source::include_src: false

docker::version: '0.11.1'
